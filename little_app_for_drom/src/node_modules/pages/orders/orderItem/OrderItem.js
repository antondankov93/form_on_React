import React from 'react';
import s from './OrderItem.module.scss'

export const OrderItem = ({propsList}) => {
    let {city, day, time, name, phone} = propsList;
    return (
        <>
            <p className={s.tableData}>{city}</p>
            <p className={s.tableData}>{day}</p>
            <p className={s.tableData}>{time}</p>
            <p className={s.tableData}>{phone}</p>
            <p className={s.tableData}>{name}</p>
        </>


    )
}