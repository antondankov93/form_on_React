import React from 'react';
import {Modal} from "react-bootstrap";

export const ModalWindow = ({show, onHide}) => {
    return (
        <Modal
            show={show}
            onHide={onHide}
            size="lg"
            aria-labelledby="contained-modal-title-vcenter"
            centered
        >
            <Modal.Header closeButton>
            </Modal.Header>
            <Modal.Body>
                <p style={{textAlign: 'center'}}>
                    <b>Ваша заявка успешно создана!</b>
                </p>
            </Modal.Body>
        </Modal>
    )
}